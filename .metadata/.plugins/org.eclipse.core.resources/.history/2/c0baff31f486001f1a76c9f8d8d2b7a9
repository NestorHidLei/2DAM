package mainApp;

import java.sql.Timestamp;
import java.util.ArrayList;
import java.util.List;
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.FileReader;
import java.io.FileWriter;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

import conexion.Conexion;
import resources.Profesor;

public class MainApp {
	
	private static final String INSERTAR_PROFESORES = "";
	private static final String INSETAR_ASIGNATURAS = "";

	public static void main(String[] args) {
		String linea = "";
		List<Profesor> listaProfesores = new ArrayList<>();
		List<Asignatura> listaAsignaturas = new ArrayList<>();
		try(Connection connect = Conexion.conectar()){
			PreparedStatement st = connect.prepareStatement("");
			try (BufferedReader br = new BufferedReader(new FileReader("Fichero.csv"))) {
				while(linea != null) {
					linea = br.readLine();
					if (linea.startsWith("P")) {
						
					}
				}
					
				}
			}
			
			
			
			
		} catch (Exception e) {
			
		}
	}
