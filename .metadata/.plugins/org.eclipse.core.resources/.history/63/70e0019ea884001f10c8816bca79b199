package actividad203.seguridad;

import java.util.Arrays;

public class CifraAESApp {
	
	private final String INSTANCIAFABRICACLAVE = "";
	private final String MENSAJE_POR_DEFECTO = "Mensaje de prueba";
	private final char[] PASSWORD_POR_DEFECTO = "pass123".toCharArray();
	private final byte[] SALT = "prueba de cadena para salt".getBytes();
	private final int ITERATIONS = 100;
	private final int KEYSIZE = 256;
	private String mensaje;
	private char[] password;
	
	public CifraAESApp(String[] args) {
		this.mensaje = MENSAJE_POR_DEFECTO;
		this.password = PASSWORD_POR_DEFECTO;
		if (args.length == 2) {
			this.mensaje = args[0];
			this.password = args[1].toCharArray();
		}
	}
	

	public static void main(String[] args) {
		String mensaje;
		String password;
		if (args.length == 0) {
			mensaje = "Mensaje de prueba";
			password = "pass123";
		} else {
			mensaje = args[0];
			password = args[1];
		}
		
		try {
			
		}
	}

}
